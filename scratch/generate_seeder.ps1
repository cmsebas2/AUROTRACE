502FO03
503FO04
503FO06
503FO07
502AN01
502AN03
502AN04
502AN02
506FO12
510FO14
303AM01
503AM01
302PE01
308PE02
303AU01
303AU02
306AU03
306AU04
310AU05
310AU06
312AU07
407AU01
410AU02
410AU03
503AU02
502AU01
307AT01
405AT01
408AT02
303AV01
311AV05
308AV03
403AV01
306AV02
405PE01
301AN01
304AZ01
304AZ02
410AZ01
302AC01
302AC02
302AC03
306AC04
305AC05
305AC06
305AC07
308AC08
307AC09
307AC10
307AC11
309AC12
309AC13
310AC15
403AC03
403AC04
405AC09
405AC10
406AC11
410AC12
410AC13
509AC11
503AC05
503AC06
503AC07
503AC08
303BO01
310BO03
403BO01
308BO02
407BO03
407BO02
407BO04
504BO03
507BO04
507BO05
601AZ01
502BE01
403CN01
503PE01
509PE02
304CA01
303CF01
406CN02
406CN03
305FO05
506CN02
304CN01
307CN02
310CN03
506CN01
211CI10
301CI01
302CI02
302CI03
306CI07
305CI04
305CI05
306CI08
306CI09
307CI10
307CI11
308CI12
309CI13
309CI14
310CI15
310CI16
310CI17
310CI18
310CI20
311CI19
311CI21
402CI02
311CI22
402CI01
402CI04
402CI05
402CI03
405CI07
405CI08
405CI09
402UG01
407DN01
405CI10
405CI11
405CI12
407CI13
407CI14
407CI15
408CI17
408CI18
305CI06
412CI26
410CI19
410CI20
603CI05
501CI02
501CI01
603CI06
509CI17
410CI21
506CI11
501CI03
501CI04
501CI05
503CI07
503CI08
504CI10
504CI09
511CT06
302CX01
411CX05
506CX17
506CX16
506CX15
506CX13
506CX14
506CX12
504CX05
504CX06
504CX07
305DN01
301UG01
502UG01
502CX01
502CX02
502CX03
502CX04
311CA02
309CP01
410CP03
410CP02
408CP01
410CT10
412CT11
304CT01
309CT06
310CT07
312CT08
303CT03
304CT02
307CT05
402CT01
402CT02
403CT04
409CT09
409CT07
407CT05
301FI01
304FI02
309FI03
406FI01
406FI02
409FI03
410FI04
502FI01
502FI02
305FS01
507FS01
412FS01
305FS03
305FS02
"@

$whitelist = [System.Collections.Generic.HashSet[string]]::new([System.StringComparer]::OrdinalIgnoreCase)
$whitelistRaw.Split("`n") | ForEach-Object {
    $item = $_.Trim()
    if ($item -ne "") { [void]$whitelist.Add($item) }
}

$rawLines = Get-Content -Path "d:\Sebastian\Escritorio\AUROTRACE\aurotrace_codigo\lotes.csv" -Encoding UTF8

$batches = [ordered]@{}

# Parse Section 1 (Lines 1 to 520)
for ($i = 1; $i -lt 520; $i++) {
    $line = $rawLines[$i]
    if ([string]::IsNullOrWhiteSpace($line)) { continue }
    $cols = $line.Split(",")
    if ($cols.Count -ge 3) {
        $op = $cols[0].Trim('"').Trim()
        $product = $cols[1].Trim('"').Trim()
        $lote = $cols[2].Trim('"').Trim()
        $brCompleto = if ($cols.Count -ge 4) { $cols[3].Trim('"').Trim() } else { "NO" }
        $obs = if ($cols.Count -ge 5) { $cols[4].Trim('"').Trim() } else { "" }
        $ubicacion = if ($cols.Count -ge 6) { $cols[5].Trim('"').Trim() } else { "" }

        if ($lote -ne "" -and $whitelist.Contains($lote)) {
            if (-not $batches.Contains($lote)) {
                $batches[$lote] = @{
                    op = $op
                    product = $product
                    lote = $lote
                    brCompleto = $brCompleto
                    obs = $obs
                    ubicacion = $ubicacion
                    fechaFab = ""
                    fechaVenc = ""
                    tamanoLote = ""
                    maquilador = ""
                    presentaciones = @()
                }
            } else {
                if ($op -ne "" -and $batches[$lote].op -eq "") { $batches[$lote].op = $op }
                if ($product -ne "" -and $batches[$lote].product -eq "") { $batches[$lote].product = $product }
                if ($obs -ne "" -and $batches[$lote].obs -eq "") { $batches[$lote].obs = $obs }
                if ($ubicacion -ne "" -and $batches[$lote].ubicacion -eq "") { $batches[$lote].ubicacion = $ubicacion }
            }
        }
    }
}

# Parse Section 2..5 (Lines 520 to 1988)
for ($i = 520; $i -lt $rawLines.Count; $i++) {
    $line = $rawLines[$i]
    if ([string]::IsNullOrWhiteSpace($line)) { continue }
    $cols = $line.Split(",")
    if ($cols.Count -ge 3) {
        foreach ($col in $cols) {
            $c = $col.Trim('"').Trim()
            if ($whitelist.Contains($c)) {
                $lote = $c
                if (-not $batches.Contains($lote)) {
                    $batches[$lote] = @{
                        op = "OP-$lote"
                        product = $cols[0].Trim('"').Trim()
                        lote = $lote
                        brCompleto = "SI"
                        obs = ""
                        ubicacion = ""
                        fechaFab = ""
                        fechaVenc = ""
                        tamanoLote = ""
                        maquilador = ""
                        presentaciones = @()
                    }
                }
                
                $prod = $cols[0].Trim('"').Trim()
                $pres = if ($cols.Count -ge 2) { $cols[1].Trim('"').Trim() } else { "" }
                $f1 = if ($cols.Count -ge 4) { $cols[3].Trim('"').Trim() } else { "" }
                $f2 = if ($cols.Count -ge 6) { $cols[5].Trim('"').Trim() } else { "" }
                $tam = if ($cols.Count -ge 7) { $cols[6].Trim('"').Trim() } else { "" }
                $maq = if ($cols.Count -ge 8) { $cols[7].Trim('"').Trim() } else { "" }

                if ($prod -ne "" -and ($batches[$lote].product -eq "" -or $batches[$lote].product -match "^FECHA")) { $batches[$lote].product = $prod }
                if ($f1 -ne "" -and $batches[$lote].fechaFab -eq "") { $batches[$lote].fechaFab = $f1 }
                if ($f2 -ne "" -and $batches[$lote].fechaVenc -eq "") { $batches[$lote].fechaVenc = $f2 }
                if ($tam -ne "" -and $batches[$lote].tamanoLote -eq "") { $batches[$lote].tamanoLote = $tam }
                if ($maq -ne "" -and $batches[$lote].maquilador -eq "") { $batches[$lote].maquilador = $maq }
                if ($pres -ne "" -and $pres -ne $lote -and -not ($batches[$lote].presentaciones -contains $pres)) {
                    $batches[$lote].presentaciones += $pres
                }
            }
        }
    }
}

# Generate Migration Code
$phpCode = @"
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;
use App\Models\MaquilaProductionOrder;
use App\Models\MaquilaItem;
use App\Models\Maquilador;
use App\Models\BatchRecordArchiveLocation;
use Carbon\Carbon;

return new class extends Migration
{
    public function up(): void
    {
        if (!Schema::hasTable('maquila_production_orders')) {
            return;
        }

        Schema::table('maquila_production_orders', function (\Illuminate\Database\Schema\Blueprint `$table) {
            if (!Schema::hasColumn('maquila_production_orders', 'lote')) {
                `$table->string('lote')->nullable();
            }
            if (!Schema::hasColumn('maquila_production_orders', 'op')) {
                `$table->string('op')->nullable();
            }
            if (!Schema::hasColumn('maquila_production_orders', 'numero_odm')) {
                `$table->string('numero_odm')->nullable();
            }
            if (!Schema::hasColumn('maquila_production_orders', 'producto_nombre')) {
                `$table->string('producto_nombre')->nullable();
            }
        });

        if (Schema::hasTable('maquila_items')) {
            Schema::table('maquila_items', function (\Illuminate\Database\Schema\Blueprint `$table) {
                if (!Schema::hasColumn('maquila_items', 'sdm')) {
                    `$table->string('sdm')->nullable();
                }
                if (!Schema::hasColumn('maquila_items', 'lote_fisico')) {
                    `$table->string('lote_fisico')->nullable();
                }
                if (!Schema::hasColumn('maquila_items', 'presentacion')) {
                    `$table->string('presentacion')->nullable();
                }
                if (!Schema::hasColumn('maquila_items', 'cantidad_programada')) {
                    `$table->decimal('cantidad_programada', 12, 3)->default(1000);
                }
                if (!Schema::hasColumn('maquila_items', 'unidad_medida')) {
                    `$table->string('unidad_medida', 20)->default('UND');
                }
            });
        }

        DB::beginTransaction();
        try {
            // Ensure default Maquilador
            `$defaultMaquilador = Maquilador::firstOrCreate(
                ['nombre' => 'AUROFARMA MAQUILADOR GENERAL'],
                ['nit' => '800000000-1']
            );

            // Ensure Admin User for foreign key constraint
            `$adminUser = \App\Models\User::firstOrCreate(
                ['email' => 'admin@aurofarma.com'],
                [
                    'name' => 'Administrador Aurofarma',
                    'password' => \Illuminate\Support\Facades\Hash::make('password'),
                    'role' => 'admin'
                ]
            );

            `$data = [
"@

foreach ($loteKey in $batches.Keys) {
    $b = $batches[$loteKey]
    $op = ($b.op -replace "'", "\'").Trim()
    if ([string]::IsNullOrWhiteSpace($op)) { $op = "OP-$loteKey" }
    $prod = ($b.product -replace "'", "\'").Trim()
    if ([string]::IsNullOrWhiteSpace($prod)) { $prod = "PRODUCTO MAQUILA" }
    $lote = ($b.lote -replace "'", "\'").Trim()
    $brComp = if ($b.brCompleto -match "SI") { "SI" } else { "NO" }
    $obs = ($b.obs -replace "'", "\'").Trim()
    $ub = ($b.ubicacion -replace "'", "\'").Trim()
    $fFab = ($b.fechaFab -replace "'", "\'").Trim()
    $fVenc = ($b.fechaVenc -replace "'", "\'").Trim()
    $tam = ($b.tamanoLote -replace "'", "\'").Trim()
    $maq = ($b.maquilador -replace "'", "\'").Trim()

    $presArray = @()
    foreach ($p in $b.presentaciones) {
        $pClean = ($p -replace "'", "\'").Trim()
        if (![string]::IsNullOrWhiteSpace($pClean)) {
            $presArray += "'$pClean'"
        }
    }
    $presStr = $presArray -join ", "

    $phpCode += @"
                '$lote' => [
                    'op' => '$op',
                    'producto' => '$prod',
                    'lote' => '$lote',
                    'br_completo' => '$brComp',
                    'observaciones' => '$obs',
                    'ubicacion' => '$ub',
                    'fecha_fab' => '$fFab',
                    'fecha_venc' => '$fVenc',
                    'tamano_lote' => '$tam',
                    'maquilador' => '$maq',
                    'presentaciones' => [$presStr]
                ],

"@
}

$phpCode += @"
            ];

            foreach (`$data as `$lote => `$b) {
                // Find or create Maquilador
                `$maqName = !empty(`$b['maquilador']) ? `$b['maquilador'] : 'AUROFARMA MAQUILADOR GENERAL';
                `$maquilador = Maquilador::firstOrCreate(
                    ['nombre' => `$maqName],
                    ['nit' => '900' . rand(100000, 999999) . '-1']
                );

                // Find existing or create Order
                `$order = MaquilaProductionOrder::where('lote', `$lote`)
                    ->orWhere('numero_odm', 'ODM-' . `$lote`)
                    ->first();

                if (!`$order) {
                    `$order = MaquilaProductionOrder::create([
                        'numero_odm' => 'ODM-' . `$lote`,
                        'op' => `$b['op'],
                        'lote' => `$lote`,
                        'tipo_producto' => 'producto_terminado',
                        'maquilador_id' => `$maquilador->id`,
                        'usuario_creador_id' => `$adminUser->id`,
                        'fecha_creacion' => Carbon::now(),
                        'fecha_envio_maquila' => Carbon::now(),
                        'estado' => `$b['br_completo'] === 'SI' ? 'liquidada' : 'en_proceso',
                        'observaciones' => `$b['observaciones'],
                    ]);
                } else {
                    `$order->update([
                        'op' => `$b['op'],
                        'maquilador_id' => `$maquilador->id`,
                        'observaciones' => `$b['observaciones'],
                    ]);
                }

                // Add Items/Presentations if any
                if (!empty(`$b['presentaciones'])) {
                    foreach (`$b['presentaciones'] as `$index => `$presName) {
                        MaquilaItem::firstOrCreate(
                            [
                                'maquila_production_order_id' => `$order->id`,
                                'codigo_item' => 'SKU-' . `$lote . '-' . (`$index + 1`),
                            ],
                            [
                                'sdm' => 'SDM-' . `$lote`,
                                'descripcion_producto' => `$b['producto'] . ' - ' . `$presName,
                                'lote_fisico' => `$lote`,
                                'presentacion' => `$presName`,
                                'cantidad_programada' => 1000,
                                'unidad_medida' => 'UND',
                            ]
                        );
                    }
                } else {
                    MaquilaItem::firstOrCreate(
                        [
                            'maquila_production_order_id' => `$order->id`,
                            'codigo_item' => 'SKU-' . `$lote . '-1',
                        ],
                        [
                            'sdm' => 'SDM-' . `$lote`,
                            'descripcion_producto' => `$b['producto'],
                            'lote_fisico' => `$lote`,
                            'presentacion' => 'PRESENTACION ESTANDAR',
                            'cantidad_programada' => 1000,
                            'unidad_medida' => 'UND',
                        ]
                    );
                }

                // Create or Update Archive Location for /consultas-br
                if (Schema::hasTable('batch_record_archive_locations')) {
                    `$ubNum = is_numeric(`$b['ubicacion']) ? (int)`$b['ubicacion'] : 1;
                    if (`$ubNum < 1) `$ubNum = 1;
                    if (`$ubNum > 210) `$ubNum = 210;

                    `$nivel = (int)ceil(`$ubNum / 42);
                    if (`$nivel < 1) `$nivel = 1;
                    if (`$nivel > 5) `$nivel = 5;

                    `$slot = (`$ubNum % 4) + 1;
                    `$cara = (`$ubNum % 2 !== 0) ? 'VISIBLE' : 'POSTERIOR';

                    BatchRecordArchiveLocation::updateOrCreate(
                        [
                            'lote' => `$lote`,
                        ],
                        [
                            'rack' => 'RACK 1',
                            'nivel' => `$nivel`,
                            'archivador_numero' => `$ubNum`,
                            'slot' => `$slot`,
                            'cara' => `$cara`,
                            'op_number' => `$b['op'],
                            'producto_nombre' => `$b['producto'],
                            'tipo_origen' => 'MAQUILA',
                            'maquila_production_order_id' => `$order->id`,
                            'fecha_archivo' => Carbon::now(),
                            'notas' => `$b['observaciones'] ? `$b['observaciones'] : 'BR Completo: ' . `$b['br_completo'],
                        ]
                    );
                }
            }

            DB::commit();
        } catch (\Throwable `$e) {
            DB::rollBack();
            throw `$e;
        }
    }

    public function down(): void
    {
        // No destructivo
    }
};
"@

$targetPath = "d:\Sebastian\Escritorio\AUROTRACE\aurotrace_codigo\database\migrations\2026_09_08_210000_seed_historic_batch_records.php"
[System.IO.File]::WriteAllText($targetPath, $phpCode, [System.Text.Encoding]::UTF8)

Write-Output "Migration written successfully to $targetPath with $($batches.Count) lotes."
